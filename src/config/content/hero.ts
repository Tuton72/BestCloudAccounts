/**
 * Homepage hero copy. Kept as plain data so marketing copy can be edited
 * without touching the Hero component.
 *
 * The heading renders as: `headline` (white, own line), then
 * `headlinePrefix` (white) followed by `headlineHighlight` (gradient) on
 * the next line — only the highlighted phrase uses the accent gradient.
 */
export const heroContent = {
  eyebrow: "AWS Accounts & Credit Marketplace",
  headline: "Premium Cloud Accounts",
  headlinePrefix: "Built for",
  headlineHighlight: "Serious Workloads.",
  subheadline:
    "Browse AWS accounts, AI-ready AWS accounts, and AWS credit packages with transparent pricing. Choose a configuration, place your order, and get professional support along the way.",
  primaryCta: { label: "Browse AWS Products", href: "/aws" },
  secondaryCta: { label: "How It Works", href: "/how-it-works" },
};
